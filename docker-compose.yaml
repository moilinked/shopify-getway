services:
  shopify-gateway:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: shopify-gateway
    ports:
      - "9997:9997"
    volumes:
      - ./config:/app/config:ro
      - ./logs:/app/logs
    restart: unless-stopped
