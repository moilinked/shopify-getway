version: "3.9"

services:
  shopify-gateway:
    image: shopify-gateway:latest
    container_name: shopify-gateway
    ports:
      - "9997:9997"
    volumes:
      - ./config:/app/config:ro
      - ./logs:/app/logs
    restart: unless-stopped
